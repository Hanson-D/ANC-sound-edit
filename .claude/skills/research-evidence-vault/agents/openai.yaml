interface:
  display_name: "Research Evidence Vault"
  short_description: "Build evidence-graded research vaults"
  default_prompt: "Use $research-evidence-vault to run a project research workflow, verify tests, and summarize evidence-graded reports."
policy:
  allow_implicit_invocation: true
